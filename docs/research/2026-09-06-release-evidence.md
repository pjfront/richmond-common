# September 6 release evidence

## Jimenez finance correction — deployed; weekly activation held

PR187 deployed source `d873d5929bbcdde8bd7a8bcc72c1d18e6d49c595` as `dpl_A4Bmi44Ei4weAiXBZy2nSf9jDEzu`, with immutable host `rtp-mg1gckey4-phillips-projects-1f180556.vercel.app`. Its wrapper passed the source/project/main/READY/production-alias checks. Exact-main [Build Check 34063432411](https://github.com/pjfront/richmond-common/actions/runs/34063432411), [Python tests 34063432399](https://github.com/pjfront/richmond-common/actions/runs/34063432399), [Web and database checks 34063432420](https://github.com/pjfront/richmond-common/actions/runs/34063432420), and [TypeScript check 34063432390](https://github.com/pjfront/richmond-common/actions/runs/34063432390) passed. The four required PR checks passed at reviewed head `da76a49a4d57ed899f9715bf08d4c28de0c96233` before merge.

The source-checked Jimenez summary now reports $60,365 cash donations for January–June, a $18,655.12 June30 cash balance, and separate $2,000 noncash support. Five July disclosures totaling $6,000 remain partial later records. Historical council donations use actual calendar years and recipient/source provenance. The earlier $68,918 display recorded below is a historical observation, not a verified campaign total; the [correction audit](../audits/2026-09-06-jimenez-finance-correction.md) explains its omissions and mixed cash/noncash assertions. Deployment alone does not attest the separately guarded canonical-data repair.

Independent authenticated postflight at 22:19:30 UTC confirmed the exact source/deployment, homepage HTTP200, health HTTP200/healthy, anonymous digest GET401, authenticated capability GET200 with `canary_ready=true` and `broadcast_ready=false`, and private/no-store responses. The malformed provider-ID check returned503 without a provider-message lookup. These were GET-only checks: zero email POSTs and no recipient or publication changes.

PR185 remains draft, unmerged and undeployed. Merging this release into it preserved the eleven original activation behavior/test patches byte-for-byte. The exact approved two-publication email fixture retained both HTML/text hashes, original publication versions and source order. Local integration passed581 frontend tests with two workers, TypeScript checking, and107 focused Python tests including real Bash/jq execution. The initial default-worker frontend run had local worker exits; the bounded rerun completed successfully. Fresh exact-head CI is required before the held launch can merge. The [launch runbook](../weekly-digest-launch.md) preserves the original one-attempt authorization, September7 00:00UTC eligibility boundary and unchanged 5:10p.m. Richmond follow-up.

## Anderson finance update — deployed; weekly activation held

PR186 deployed source `fa39bc21240fa9be07d05397efe769ed39cabcc4` as `dpl_E6mF7NiopUPcTpowuQkrKfvQUX2X`, with immutable host `rtp-mxslfxs4x-phillips-projects-1f180556.vercel.app`. The deployment wrapper passed its source/project/main/READY/production-alias checks. [Main Build Check 34061251404](https://github.com/pjfront/richmond-common/actions/runs/34061251404) and [main Python tests 34061251443](https://github.com/pjfront/richmond-common/actions/runs/34061251443) passed. All four PR checks passed on reviewed head `d5698033f0b0c90fed81ae147115de6f9346d4a3` before merge.

Authenticated postflight at 21:34:21 UTC independently confirmed the exact source and deployment, homepage HTTP200, health HTTP200/healthy, anonymous digest GET401, and authenticated capability GET200 with `canary_ready=true` and `broadcast_ready=false`. Digest responses remained private/no-store; the malformed provider-ID check returned503. The read-only canary preflight confirmed that the representative week was not yet selectable and no local attempt receipt existed. These checks made zero email POSTs or provider-message lookups. Absence of a local receipt is not proof that no remote attempt occurred; inspect the workflow history again before the authorized send.

The held activation branch was synchronized with this main release. Its original eleven behavior/test patches remained byte-identical; both release histories were preserved. Local integration passed516 frontend tests, TypeScript checking and107 focused Python tests, including the actual Bash/jq workflow harness. Fresh required CI must pass on the final PR185 head before the canary-verified activation can merge. The [launch runbook](../weekly-digest-launch.md) retains the one-attempt hold and the unchanged 5:10p.m. Richmond follow-up.

## Provider verification update — deployed; weekly activation held

PR184 deployed source `59615f9ced439f4094e870fa6c57cbf9222451f8` as `dpl_rfdoTHUgSiMFAT3dXefTG38AGoSH`, with immutable host `rtp-mjj556l13-phillips-projects-1f180556.vercel.app`. The deployment wrapper passed its source/project/ref/READY/alias proofs. Independent authenticated control-plane checks at 20:33 and 20:34 UTC agreed on that exact source and deployment for the production domain. [Main Build Check 34057910960](https://github.com/pjfront/richmond-common/actions/runs/34057910960) and [main Python tests 34057910921](https://github.com/pjfront/richmond-common/actions/runs/34057910921) passed; all four PR checks passed before merge.

Read-only production postflight at 20:34 UTC passed: homepage HTTP200, health HTTP200/healthy, anonymous digest GET401, authenticated capability GET200 with `canary_ready=true` and `broadcast_ready=false`, and a malformed provider-ID query503. All digest GET responses were private/no-store. No email POST or real provider-message lookup was made. The scoped proof endpoint can verify the existing configured canary's exact sender, destination, subject, body hashes and provider state without exposing its content or credentials.

The user authorized one representative canary and subsequent Monday activation after verification. That August31–September6 UTC week becomes eligible at September7 00:00UTC (September6 5p.m. PDT). A separate paired activation remains held for that check; see the [launch record](../weekly-digest-launch.md). Preparation passed494 frontend tests, TypeScript checking,118 focused Python tests with one existing Windows-only skip, and22 private once-only-caller test cases. These tests used mocked email providers. No subscriber broadcast or canary send has occurred during this preparation, and no new migration or recipient-preference change was made.

## Resident production release — independently verified

The deployed application source is `97abc9bd5c4ae81911b90034e2f10823dd5df8af`, deployment `dpl_5QCfFa2CLET6ABPMW4ff9BGqrTc1`, READY since September 6 at 17:34:17 UTC. Its immutable host is `rtp-9k6hbft4x-phillips-projects-1f180556.vercel.app`; the public domain is [richmondcommons.org](https://richmondcommons.org). A later documentation-only commit may advance `main` without changing this deployed source. This receipt does not trigger another application deployment.

At 17:35:39 UTC, an independent authenticated Vercel API check retrieved the exact deployment ID and separately resolved the production domain. Both returned the same deployment, pinned project `prj_Y0sIBsC2DKkl4lsoKbS11Y3cFTz4`, exact GitHub SHA, `main` ref, `githubDeployment=1`, production target, READY state, and no alias error.

The repaired deployment wrapper exited successfully with its own source/project/ref/READY/alias proofs. Its immutable archive estimate was 2,900 KB across 435 files; the CLI reported a 969.8 KB upload and the remote build downloaded 437 files. The pinned local deployment CLI was 59.1.4; Vercel's managed build reported CLI 59.11.7. These are different execution environments, not evidence of different source bytes. Exact-main [Build Check 34048867103](https://github.com/pjfront/richmond-common/actions/runs/34048867103), [Python tests 34048867328](https://github.com/pjfront/richmond-common/actions/runs/34048867328), and [Web Tests 34048867249](https://github.com/pjfront/richmond-common/actions/runs/34048867249) passed. The prior production target was `dpl_DJyqAKnwN7UPZ66cfJxoPo2ZuXHE`, source `7c56b6e5f92acb1553a807256ba1ae7c1e863331`; no rollback or second production deployment was needed.

Public production checks passed: homepage HTTP 200; health HTTP 200/healthy; CSV exports with 1,178 total records and 19 for committee 1490887; legal-name/FPPC search; desktop at 1440 px and mobile at 390 px without overflow or browser errors; English/Spanish switching with language retained through a story link; and the fire story's link to the exact July 28 agenda item. The follow CTA opened its prefilled fire-subject form with council updates off and weekly delivery described as not started. No form was submitted.

An independent read-only production smoke at 17:38 UTC passed 20 assertions plus browser checks. It confirmed Anderson's dated paper-filing explanation and five original links; Jimenez's indexed 2026 gross/net receipts of $68,918; four November independent-spending cells with no matching published records rather than inferred zero totals; both published version-2 briefs; and three correct-direction RPOA → Safe Richmond $30,000 receipts. Eleven original filing links returned HTTP 200/application/pdf. Those link checks establish availability, not a fresh PDF-content audit or complete finance coverage.

## Access and maintenance release — deployed

Production was independently verified at `7c56b6e5f92acb1553a807256ba1ae7c1e863331`, deployment `dpl_DJyqAKnwN7UPZ66cfJxoPo2ZuXHE`, on September 6. The Vercel API confirmed READY production state, the pinned Richmond project, exact GitHub commit/ref metadata, and the `richmondcommons.org` alias. The homepage and `/api/health` returned HTTP200; health reported healthy.

The immediately preceding production deployment was `dpl_3Fit9sx7D97BgAbA3iqsRfbjSfUp`, source `0ff9fd50443d8d13e15a4d83845b2997cfc1054a`. This earlier release included protected operator reads, retired unfinished public directory/detail paths, council accessibility and contrast repairs, bounded sitemap/discovery, contained subscriber delivery and recap recovery, and release/preview tooling. The subsequent resident release is recorded above and below.

Build Check main-push run34043577214 passed on the deployed commit. PR171's schema comparison, build, Python, frontend and database permission checks passed. Its generated types came directly from production through pinned SupabaseCLI2.112.0; the schema object did not change, apart from the hosted PostgREST version metadata. The remaining generated changes were helper formatting.

The deploy wrapper uploaded an immutable Git archive, 2,676KB across388 estimated files, using pinned VercelCLI59.1.4. Its final stdout URL parser rejected the CLI output after deployment completed. Independent control-plane and HTTP verification resolved the ambiguity; the deployment was neither repeated nor rolled back. The captured stdout bytes were not retained, so their precise format is unknown.

Migration147 was applied atomically from committed SQL, with an exact statement recorded in the migration ledger. Its SHA256 is `f91813f86e8eb667077986c3ed7f44ebf841e45b1b14ed209c2f11d023c52a15`. Anonymous/authenticated access to private operator tables is denied; neighborhood councils remain publicly readable with no public mutation grants. Service access and existing row counts were preserved. Migrations145 and146 were already applied before this release. Migration136 remains applied; forbidden134 remains absent. No database rollback was performed.

The production server has the required sensitive service-role environment key. An authenticated production inbox smoke test could not be completed because this checkout has no local operator password; no credentials were changed or requested. Route authorization and service-client behavior passed executable tests, and the private table grants passed actual PostgreSQL checks.

## Resident and finance release — database and website live

PR172 merged as `f4319066b4a0793e47e5d3497a01669ed939cd4c`. Its H0 was `6aeb13cc41c38ebf30c3f13b6d5f3a0a3940d3d6`; generated-types-only H1 was `f47aeb63eccb17280b7bbcc17044443eda6e8a63`. All required Python, web, database permissions and build checks passed, as did Schema Type Gate and the schema-drift rerun. The integrated frontend passed370 tests. Disposable PostgreSQL checks cover finance evidence immutability, public roles and views, versioned publication, source invalidation, batch persistence and the exact legacy repair with complete rollback after an injected final-insert failure.

The isolated schema rehearsal and preview succeeded in run34044630503. The preview showed correct empty-coverage behavior, readable desktop and mobile layouts, and no browser errors. PR-close cleanup run34045675084 confirmed the isolated database was deleted and nine scoped Vercel environment entries were removed. No retained preview compute remains from this rehearsal.

Migrations148 and149 were applied atomically from the merged committed SQL on September6. Their SHA256 values are `449da20aaa14c8e8d56f8fe0a80e2b00611bee2913fc3f09423c79c65c25a9ca` and `a1d7c066f254565c4bf3b6a96dede15fe2a16aff939ac69d750e45e703309b44`. Effective anonymous/authenticated table, view and review-RPC permissions passed against production; existing row counts were preserved. Forbidden134 remains absent.

The production import stored1,265 source assertions and1,178 canonical public events from seven electronic form types. It archived13 Form496 PDFs and the original transaction payloads (2,564,532 bytes total), used115 metadata requests, and made no model calls. Twelve assertions remain held for reconciliation: nine with ambiguous cross-report multiplicity and three with date disagreements. Coverage is explicitly partial, searched January1–September6,2026; paper filings and periodic independent spending are not included. All32 rapid independent-spending records lack a source-established election date and are not presented as November spending totals.

The guarded legacy repair removed11 reversed projections after preserving their source evidence, restored one missing recipient-backed $30,000 receipt, and verified the existing Safe Richmond committee ID. It applied only the freshly previewed state hash `7e40917c873e157dd60f3b23e5fb3bc505fedbfe44cee8c970eeaadfce4763be`, with source-evidence hash `33357d66c6995b1e419e489ca40efb29e8cbc526200a7a2735b29ba7230f6fbb`. A subsequent read-only preview found zero remaining corrections. The public projection independently confirms three distinct Richmond Police Officers Association → Safe Richmond receipts on May12,18,29 totaling$90,000.

The private source-packet producer created six engineering review packets and two editorial drafts. The six engineering comparisons remain investigations; their generic approval action does not repair the finance ledger. Retained filing periods and amendment metadata can enrich them without new API calls, but made-versus-received dates and overlapping filing periods still require source verification before merging records.

Codex, acting under the user's delegated authorization, visually checked the three exact original Form 497 PDFs, edited both editorial drafts, and published content version 2 through the versioned review contract. This was an authorized AI source check, not a human review. Public pages and email templates say “AI-written; checked against linked sources.”

| Published update | Original PDFs checked |
| --- | --- |
| [Safe Richmond Neighborhoods reports $50,000 August 24 receipt](https://richmondcommons.org/updates/d1976161-9fc6-4467-8075-aacc7d4e1ae7?version=2&published=2026-09-06T17%3A02%3A02.96931%2B00%3A00#brief-d1976161-9fc6-4467-8075-aacc7d4e1ae7-v2) | [217287603](https://netfile.com/Connect2/api/public/image/217287603), SHA-256 `72900a033d3ca6aad89e86a82dfef8c74fd73d95edde764b762e254516a03df0` |
| [Safe Richmond Neighborhoods reports two receipts totaling $45,000](https://richmondcommons.org/updates/9291c927-0646-47da-94ef-5801786f0cd2?version=2&published=2026-09-06T17%3A02%3A03.002021%2B00%3A00#brief-9291c927-0646-47da-94ef-5801786f0cd2-v2) | [217338356](https://netfile.com/Connect2/api/public/image/217338356), SHA-256 `d99056699bc02fe1e939e1fc73a4336c80716dcac4ecd22180e07c1ef1067ccb`; [217386509](https://netfile.com/Connect2/api/public/image/217386509), SHA-256 `712c1ab9917436f2db7ca91c13cd7a6bd84ea8b3e1853682da84b330c4d5cc83` |

The briefs retain the full recipient identity, exact reported donor names/FPPC IDs, receipt dates/amounts, and distinct source filing dates. They describe receipts into the named committee, not direct candidate-controlled fundraising, spending, influence, wrongdoing, or original PAC funders. A producer replay found all eight inputs unchanged, created/refreshed/invalidated zero packets, and preserved the edited publications. No email was sent.

An authenticated local application read against the production database returned HTTP200; anonymous access returned401. The local test used an ephemeral password and the existing service credential; production authentication configuration was unchanged.

Browser and HTTP checks against the imported database verified legal-name search, exact FPPC filtering, matching CSV exports (1,178 total records;19 for Safe Richmond), original source links, mobile layout, and story-to-exact-agenda navigation. Browser-tool clicks required explicit scrolling to visible links. The raw evidence endpoint returned401 to the anonymous key. The subsequent production deployment, authenticated source/alias attestation, and public checks passed as recorded above.

PR174 merged the structured VercelCLI output parser with authenticated deployment proofs unchanged. PR175 merged the source-backed September15 council-return notice and a bounded scanned-PDF persistence repair. The single-document production verification used unmodified merged source `fddd90d249d40c8618e8c0a6a5ef712a92691cb7` at 16:42:33 UTC. It fetched only [official archive ADID 17838](https://www.ci.richmond.ca.us/Archive.aspx?ADID=17838): 673,151 PDF bytes, SHA-256 `342ef7949272679545e3ef0baed038e55046639f133af9c04924ae972a4c7b7b`. Exactly one document was retained under a one-new-PDF/2 MiB cap; replay inserted zero rows, wrote zero new bytes, and preserved its complete source snapshot. No meeting row or existing document changed. Deterministic extraction found no text, so `raw_text` remains NULL and `text_extraction_status=unavailable`; no OCR, model, date inference, or downstream dispatch was performed. The normal archive cap remains 20 new scans/32 MiB per run, and fresh runners still re-fetch the full inventory. That efficiency problem is unresolved.

PR181 merged as `52a294b7fd3d99a12fc9f09f5389b587bb282b81`. It adds Anderson's paper-filing coverage explanation and original report links with a bounded two-request metadata refresh for the exact verified filer. Identity/period checks and a dated fallback prevent an unavailable source from becoming a zero-fundraising finding. It does not extract paper money. The standard daily finance/review job is separate from subscriber email delivery and uses no model calls.

PR182 merged as `97abc9bd5c4ae81911b90034e2f10823dd5df8af`. After repeated early preview restarts, it requires the exact branch to be at least 60 seconds old and two healthy read-only samples of the same PostgreSQL postmaster with at least 30 seconds of uptime before baseline writes. Polling is capped at 180 seconds; in-flight requests retain their existing timeout. All 217 focused tests passed locally and independently. This barrier has not yet been exercised by a subsequent live bootstrap, and a later platform restart remains possible. Writes stay single-attempt and identity/clone/data/cleanup protections remain intact.

## Subject follows — applied and deployed; weekly mail inactive

PR176 merged as `99d69dc213cefb292ef89a1eaf4bfd988183cefb` with authoritative generated types. PR179 merged subscriber access containment and accurate AI/source-check attribution as `8db732ff3f3b92f2b875396da31ca6d9d9b3907b`, after all eight checks and successful second bootstrap run34047440037. No generated types were hand-edited.

Migrations150 and151 were applied together from that merged source in one transaction, recording the exact SQL under committed ledger versions `20260906015000` and `20260906015100`. Their SHA-256 hashes are `3b7f2a0d9ee8239152329bdb0589f4c15de1d5b15dac139f0bbda18efb51c3e4` and `3c0cab3c32da972669f57333c116335c7630ab2947d85ed00d33c2925bdc4960`. Replay applied neither again and passed the same checks.

Production postflight confirmed all seven table privileges denied to anonymous/authenticated roles on private subscription tables, unchanged service table grants, service-only subscription RPCs, and no direct execution of the renamed legacy claim core even by the service role. Full-row signatures of existing subscribers, preferences, deliveries, activations, briefs, and decisions were preserved except for the new consent default; existing subscribers received `receive_council_updates=true`. The executable verifier passed 200 PostgreSQL access assertions with broad hosted-style grants across pre/post-150 schemas, plus 49 consent assertions.

Subject choices and consent-aware delivery code are deployed with `DIGEST_BROADCAST_ENABLED=false`. No weekly subscriber schedule was activated; the UI says delivery has not started. No live signup, email canary, broadcast, outreach, or provider send was initiated by release verification. Remaining provider/end-to-end and activation checks are listed in [Subject follows](../subject-follows.md).

## Financial operation

Vercel's connected team reports the Hobby plan. No billing or payment-account changes were made. The support page uses the Ko-fi URL already recorded in the repository; its external checkout was not independently exercised. Voluntary donations are expressly permitted by [Vercel's fair-use guidelines](https://vercel.com/docs/limits/fair-use-guidelines#commercial-usage), checked September6. Paid products or advertising require a separate hosting/billing decision. No paid pipeline model calls, subscriber broadcasts or outreach were initiated by this release work.
