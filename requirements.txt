# Richmond Transparency Project — Phase 1 dependencies

# Environment
python-dotenv>=1.0.0

# LLM extraction
anthropic>=0.39.0

# Document scraping & parsing
requests>=2.31.0
beautifulsoup4>=4.12.0
pymupdf>=1.24.0

# Comment generation
jinja2>=3.1.0

# Socrata open data API (Transparent Richmond portal)
sodapy>=2.2.0

# Database (Phase 1 — local dev, then Supabase/Railway)
psycopg2-binary>=2.9.9

# Campaign finance — CAL-ACCESS bulk data downloaded directly via requests
# No dedicated package needed; we parse the TSV/CSV exports ourselves
