# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
venv/
.venv/

# Logs
*.log
tmp/finance/

# Operator-specific Chat Claude config (contains personal Notion IDs)
docs/CHAT-SYSTEM-PROMPT.md

# Editorial journal — moved to private Notion workspace 2026-05-01.
# File still maintained locally but no longer tracked in the public repo.
JOURNAL.md

# Competitive intelligence research — moved to private Notion workspace 2026-05-01.
# Files still maintained locally but no longer tracked in the public repo.
docs/research/competitive-intel/

# Environment / Secrets
.env
.env.local
.env.production
*.key

# Data (raw documents, extracted JSON, bulk downloads — large, not in git)
data/raw/
data/extracted/
data/calaccess/
data/netfile/
data/employees_*.json
# Also match under src/data/ where pipeline stores working files
src/data/raw/
src/data/extracted/
src/data/calaccess/
src/data/netfile/
src/data/combined_contributions.json
# Pipeline runtime cache (AMID discovery, auto-regenerates)
src/data/cache/
# Census raw data (large — processed JSON is committed, raw CSV and ZIP are not)
src/data/census/Names_2010Census.csv
src/data/census/names.zip
# Pipeline run artifacts (per-meeting output — agendas, comments, eSCRIBE scrapes)
src/data/agenda_*.txt
src/data/comment_*.html
src/data/comment_*.txt
src/data/escribemeetings_*/
# Batch API run artifacts (request/response JSONL, metadata)
src/data/batch_runs/
# Audit sidecars (large runtime artifacts, not committed)
src/data/audit_runs/
src/data/validation_reports/
# Operator-only S29 aggregate measurement packets
src/data/analytics_checkpoints/
# Paper-filed campaign finance PDFs (large scanned images, not committed — JSON data IS committed)
src/data/paper_filings/*.pdf
# Form 700 filing data and PDFs (scraped from portal, not committed)
src/data/form700/
# System health report snapshots (local trend data, not committed)
src/data/health_reports/

# Node
node_modules/
.next/
out/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Claude Code — local/ephemeral only
.claude/settings.local.json
.claude/worktrees/
.worktrees/

# Supabase CLI — local config (project-linked, not shareable)
supabase/.temp/
data/
!web/public/data/
!web/src/data/
src/data/raw/nextrequest/24-428/documents/
src/data/raw/nextrequest/24-428/searchable_corpus.json
.vercel
