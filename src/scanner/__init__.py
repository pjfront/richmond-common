"""Scanner package — extracted from conflict_scanner.py (Phase 2.2).

Signal detectors live in scanner.signals.*. Shared types, scoring, and
text utilities remain in src/conflict_scanner.py and are imported by
each signal module.
"""
